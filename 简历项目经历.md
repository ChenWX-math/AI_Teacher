# 简历项目经历素材

## 一句话介绍

基于 LangChain、DeepSeek、DashScope Embedding、Milvus Lite 和 Streamlit 构建多学科 AI 智能教师，支持角色化教学、多会话记忆和教材知识库检索增强生成。

## 简历版本

**AI 智能教师 | LangChain + DeepSeek + Milvus Lite + Streamlit**

- 使用 LangChain LCEL 组织 `Prompt → LLM` 调用链，封装 DeepSeek OpenAI-compatible API，实现可配置的学科、教师性别和教学风格角色化回答。
- 基于 `RunnableWithMessageHistory` 和自定义 JSON Chat History 实现持久化多会话，支持新建、切换、删除会话，并解决 `session_id` 配置传递问题。
- 构建端到端 RAG 流程：读取 9 个学科教材文本，使用 `RecursiveCharacterTextSplitter` 分块，通过 DashScope `text-embedding-v3` 向量化并写入 Milvus Lite。
- 实现按科目过滤的相似度检索，将教材片段、文件名和来源元数据注入 Prompt，使模型能够基于教材内容回答问题并降低无依据生成。
- 使用 Streamlit 实现流式聊天界面、知识库开关和检索数量控制；编写 Milvus 探针及多学科检索测试脚本，验证本地向量数据库的连接、写入和查询流程。

## 面试讲解顺序

1. 为什么从直接调用 OpenAI SDK 升级到 LangChain：统一 Prompt、模型、记忆和检索组件，降低业务代码耦合。
2. RAG 的数据流：教材加载 → 分块 → Embedding → Milvus → 相似度检索 → Prompt 上下文 → LLM 回答。
3. 为什么 DeepSeek 和 DashScope 分工：DeepSeek 负责生成回答，DashScope Embedding 负责将文本转换为可检索向量。
4. 为什么使用 Milvus Lite：本地文件数据库即可完成向量检索，不需要单独部署 Milvus Server，适合个人项目。
5. 会话记忆如何实现：`RunnableWithMessageHistory` 根据 `configurable.session_id` 获取对应 JSON 历史，并自动将历史消息填入 Prompt。
6. 遇到的兼容性问题：处理 `langchain-milvus` 与 `pymilvus` 动态连接别名问题，并将 Milvus 数据库迁移到纯英文路径以规避 Windows/FAISS 索引写入问题。

## 项目数据

- 9 个学科
- 27 个教材文本文件
- 74 个文本块
- 本地 Milvus Lite 向量集合
- 支持普通对话和教材 RAG 两种模式

## 30 秒口述版本

这是一个面向高中生的 AI 智能教师。我使用 Streamlit 做界面，用 LangChain 组织 Prompt、DeepSeek 模型和会话记忆。为了让回答基于教材，我把 9 个学科的资料切分后使用 DashScope Embedding 向量化，保存到 Milvus Lite。用户开启知识库后，系统会先按当前科目检索相关教材片段，再把片段注入 Prompt，让 DeepSeek 基于资料回答，同时保留每个会话的历史记录。

